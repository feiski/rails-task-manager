<h2>View all my tasks</h2>
<% @tasks.each do |task| %>
  <p><%= link_to task.name, task_path(task.id) %></p>
  <p><%= task.details %></p>
  <p><%= task.completed %></p>
<% end %>
<%# Add a new task %>
<p><%= link_to "Add a new task", new_task_path %></p>
